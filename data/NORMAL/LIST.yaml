# Typed List (Z4/Type, Generated by Z881)
$id: LIST
$ref: "#/definitions/objects/LIST"
definitions:
  objects:
    LIST:
      anyOf:
        - $ref: "#/definitions/objects/LIST_literal"
        - $ref: "Z10#/definitions/objects/Z10"
        - $ref: Z9#/definitions/objects/Z9
        - $ref: Z18#/definitions/objects/Z18
    Z1K1_for_LIST:
      # TODO: Identity must be Z881; args to Z881 must correspond to K1 type.
      $ref: Z4#/definitions/objects/Z4
    LIST_empty:
      additionalProperties: false
      properties:
        Z1K1:
          # TODO: Identity must be Z881; args to Z881 must correspond to K1 type.
          $ref: "#/definitions/objects/Z1K1_for_LIST"
      required:
        - Z1K1
      type: object
    LIST_full:
      additionalProperties: false
      properties:
        K1:
          $ref: Z1#/definitions/objects/Z1
        K2:
          $ref: "#/definitions/objects/LIST"
        Z1K1:
          # TODO: Identity must be Z881; args to Z881 must correspond to K1 type.
          $ref: "Z4#/definitions/objects/Z4"
      required:
        - K1
        - K2
        - Z1K1
      type: object
    LIST_literal:
      oneOf:
        - $ref: "#/definitions/objects/LIST_empty"
        - $ref: "#/definitions/objects/LIST_full"
