# Typed List (Z4/Type, Generated by Z881)
$id: LIST
$ref: "#/definitions/objects/LIST"
definitions:
  objects:
    LIST:
      anyOf:
        - $ref: "#/definitions/objects/LIST_literal"
        - $ref: RESOLVER#/definitions/objects/RESOLVER
    # Canonical lists: The only representation possible is Benjamin Arrays,
    # an Array where the first element is the type of the other elements
    LIST_literal:
      type: array
      additionalItems:
        $ref: Z1#/definitions/objects/Z1
      minItems: 1
      items:
        - $ref: Z4#/definitions/objects/Z4
